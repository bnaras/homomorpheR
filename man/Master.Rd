% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{Master}
\alias{Master}
\title{Abstract master class}
\usage{
Master(name = character(0), state = NULL)
}
\arguments{
\item{name}{short identifier shown in printed output.}

\item{state}{an environment for mutable bookkeeping.}
}
\value{
nothing — this class is abstract, so calling it raises an error
instead of returning an object. It exists so that \code{\link[=master_encrypt]{master_encrypt()}},
\code{\link[=master_decrypt]{master_decrypt()}} and \code{\link[=master_aggregate]{master_aggregate()}} dispatch on a common
parent. Construct a concrete master with \code{\link[=make_ckks_master]{make_ckks_master()}} or
\code{\link[=make_threshold_master]{make_threshold_master()}}.
}
\description{
Common base for \link{CKKSMaster} and \link{ThresholdMaster} (and the frozen
legacy \link{PaillierMaster}). Concrete masters carry whatever keys and
context their cryptographic backend needs; the protocol body in
\code{\link[=master_aggregate]{master_aggregate()}} uses \code{\link[=master_encrypt]{master_encrypt()}} and \code{\link[=master_decrypt]{master_decrypt()}}
generics that dispatch on the concrete master class, so the same
protocol runs over any backend.
}
