% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{set_public_params}
\alias{set_public_params}
\title{Give a party the public parameters it will encrypt under}
\usage{
set_public_params(site, ...)
}
\arguments{
\item{site}{a \link{Site}, or a user-defined subclass of \link{RemoteSite}.}

\item{...}{method-specific arguments; the built-in method takes
\code{params}, a \link{PublicParams} object.}
}
\value{
the site, invisibly. Called for its side effect.
}
\description{
The setup step of the protocol, and one of only two moments at
which anything passes between a coordinating party and a site — the
other being a round itself, which carries a query out and a
ciphertext back. A party receives its \link{PublicParams} once, here,
and from then on computes and encrypts with what it holds.
}
\details{
Called for you by \code{\link[=set_workers]{set_workers()}} and \code{\link[=make_threshold_master]{make_threshold_master()}}.
You would call it directly only when writing a \link{RemoteSite} method.
}
\section{Why this is a generic}{


Setup is a \emph{message}. For a co-located site, delivering it is an
assignment; for a remote one it is a network call that must
provision the far endpoint, and nothing in this process can do that
on the endpoint's behalf. Writing the parameters straight into a
remote proxy's \code{state} would leave the proxy looking configured
while the far end had never been told anything — a setup failure
that surfaces only much later, as a wrong answer. So the base
\link{RemoteSite} method \strong{refuses}, and a subclass must implement the
provisioning it alone knows how to do. Missing remote setup fails
closed.

What crosses is public in full: a crypto context and a public key.
There is no secret material in a \link{PublicParams} object and no
property for one to occupy.
}

\section{Reconfiguring}{


Receiving the same parameters again is harmless and allowed.
Receiving \emph{different} ones is refused. A site that silently
switched keys would keep answering its first coordinator, in a key
that coordinator cannot read — under CKKS that surfaces as an
approximation-error abort, and under BFV or BGV as a plausible
wrong integer with nothing raised. Build a fresh site instead; they
are cheap.
}

\seealso{
\code{\link[=site_params]{site_params()}} to read them back, \code{\link[=encrypt_under]{encrypt_under()}} to
use them, \link{RemoteSite} for the full remote contract.
}
