% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paillier.R
\name{paillier_keypair}
\alias{paillier_keypair}
\title{Generate a new Paillier key pair}
\usage{
paillier_keypair(modulus_bits)
}
\arguments{
\item{modulus_bits}{modulus length in bits (e.g. 1024 or 2048).}
}
\value{
a \link{PaillierKeyPair}.
}
\description{
Generates two random primes of \code{modulus_bits / 2} bits each, forms
the modulus, and returns a \link{PaillierKeyPair} containing the matching
public and private keys.
}
\examples{
keys <- paillier_keypair(1024)
ct   <- encrypt(keys@pubkey, gmp::as.bigz(42))
ct

## Only the private key recovers the cleartext:
decrypt(get_private_key(keys), ct)
}
