% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{PublicParams}
\alias{PublicParams}
\title{The public parameters a party encrypts under}
\usage{
PublicParams()
}
\value{
nothing — this class is abstract. Its concrete subclasses
are constructed for you when a party is configured.
}
\description{
Abstract base for the setup bundle a party is handed once, when it
is configured, and holds from then on. It is public in full: it is
exactly the message a coordinator would put on a wire to an
untrusted peer, and it is all anyone needs in order to encrypt.
}
\details{
The class carries \strong{no secret property}, which is what makes the
claim structural rather than a promise in prose — there is nowhere
for a secret key or a key share to travel in this object. The
concrete kinds are \link{OpenFHEParams} and, on the frozen legacy path,
\code{PaillierParams}.

Obtain the bundle a site holds with \code{\link[=site_params]{site_params()}}; encrypt with
\code{\link[=encrypt_under]{encrypt_under()}}.
}
\seealso{
\link{OpenFHEParams}, \code{\link[=site_params]{site_params()}}, \code{\link[=encrypt_under]{encrypt_under()}}
}
