% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{partial_decrypt}
\alias{partial_decrypt}
\title{One site's partial decryption of a ciphertext}
\usage{
partial_decrypt(site, ...)
}
\arguments{
\item{site}{a \link{LocalSite}, or a user-defined subclass of \link{RemoteSite}.}

\item{...}{method-specific arguments; the built-in method takes
\code{ciphertext} and \code{lead}, a flag marking the first site in the
chain.}
}
\value{
a partial decryption, to be fused by \code{\link[=decrypt]{decrypt()}}.
}
\description{
Under threshold keys no party can decrypt alone. A ciphertext is
sent to each site; each site applies \strong{its own} secret share and
returns a partial decryption, and the partials are fused (see
\code{\link[=decrypt]{decrypt()}}). The share never leaves the site, so no other
party ends up holding anything that would let it decrypt.
}
\details{
Whether a site plays the \code{lead} role is fixed by its position in the
key-generation chain, so it arrives with the request; the site does
not choose and does not need to know who is asking.

A site that cannot be reached must signal \code{\link[=site_unavailable]{site_unavailable()}}.
Because decryption is n-of-n, this loses the entire round rather
than one summand — see the availability note in \link{RemoteSite}.
}
\seealso{
\code{\link[=make_threshold_master]{make_threshold_master()}}, \code{\link[=keygen_round]{keygen_round()}}.
}
