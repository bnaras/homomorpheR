% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{keygen_round}
\alias{keygen_round}
\title{One site's step in the threshold key-generation chain}
\usage{
keygen_round(site, ...)
}
\arguments{
\item{site}{a \link{LocalSite}, or a user-defined subclass of \link{RemoteSite}.}

\item{...}{method-specific arguments; the built-in method takes
\code{cc}, the crypto context, and \code{prev_pk}, the cumulative public key
from the previous site in the chain (\code{NULL} for the lead site,
which starts the chain with a fresh keypair).}
}
\value{
the cumulative public key including this site's
contribution. Never a secret key.
}
\description{
The site derives its own secret share from its predecessor's
cumulative public key, \strong{keeps the share}, and returns only the new
cumulative public key. The share is generated at the site and is
never a return value, so no other party can hold it.
}
\details{
Called by \code{\link[=make_threshold_master]{make_threshold_master()}}, once per site, in order. A
\link{RemoteSite} implementation must do the same thing at the far end:
receive a public key, generate and retain a share locally, send a
public key back. Nothing secret crosses the wire in either
direction.
}
\seealso{
\code{\link[=make_threshold_master]{make_threshold_master()}}, \code{\link[=partial_decrypt]{partial_decrypt()}}.
}
