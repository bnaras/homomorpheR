% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{NCParty}
\alias{NCParty}
\title{A non-cooperating party}
\usage{
NCParty(name = character(0), number = integer(0), state = NULL)
}
\arguments{
\item{name}{short identifier shown in printed output.}

\item{number}{which share this NCP receives, \code{1} or \code{2}.}

\item{state}{an environment for mutable bookkeeping (the list of
sites it manages, public key). Default: a fresh empty env.}
}
\description{
Sits between the master and the sites in the non-cooperating-parties
topology. Two NCPs receive \emph{additive shares} of each site's
contribution; each NCP sums its share across sites and ships the
result to the master, who combines the two NCP totals and decrypts.
No single party — neither master nor an NCP — sees an individual
site's contribution. Use \code{\link[=make_ncparty]{make_ncparty()}} to construct.
}
