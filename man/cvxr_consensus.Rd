% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{cvxr_consensus}
\alias{cvxr_consensus}
\title{Precomputed encrypted Cox-lasso consensus-ADMM results}
\format{
A named list with components
\describe{
\item{params}{list of the run constants: \code{K} (screened probes,
100), \code{LAMBDA} (L1 penalty, 5), \code{RHO} (ADMM penalty, 50),
\code{MAX_ITER} (200), \code{TOL} (5e-3).}
\item{top_idx}{integer vector of length \code{K}; column indices into
\code{DLBCL_gex} of the top-\code{K} univariate-screened probes.}
\item{sigma_K}{numeric vector of length \code{K}; pooled standard
deviations of the screened probes, for the back-transform to
the original scale.}
\item{agg_beta}{numeric vector of length \code{K}; centralized CVXR
Cox-lasso coefficients (the ground truth), on the standardized
scale.}
\item{z_ref}{numeric vector of length \code{K}; consensus-ADMM
coefficients computed in the clear (plaintext reference).}
\item{z_enc}{numeric vector of length \code{K}; consensus-ADMM
coefficients under threshold FHE.}
\item{trajectory}{list of numeric vectors of length \code{K}; the
encrypted consensus iterate \eqn{z^t} at each ADMM iteration.}
\item{n_iter_ref, n_iter_enc}{iterations to convergence for the
plaintext and encrypted runs.}
\item{pool_agree}{list \code{mu}, \code{sigma}: max absolute disagreement
between the encrypted and plaintext pooled standardization
moments.}
\item{screen_match}{logical; whether the encrypted screen selected
the same probe set as the plaintext screen.}
}
}
\usage{
cvxr_consensus
}
\description{
Result objects from the encrypted stratified Cox-lasso
consensus-ADMM demonstration on the \link{DLBCL} / \link{DLBCL_gex} cohort:
a centralized \link[CVXR:CVXR-package]{CVXR} ground-truth fit, the same
fit recovered by consensus ADMM in the clear, and the encrypted
threshold-FHE fit that swaps only the consensus channel. The
~150-iteration ADMM runs are expensive, so they are computed once
and shipped here; the manuscript and the \code{cvxr-consensus-admm}
vignette load this object instead of recomputing, and pull their
displayed code from the generating script (see Details).
}
\details{
Generated by \code{data-raw/cvxr_consensus.R}, which sources the
canonical pipeline \code{inst/scripts/cvxr-consensus.R}. That script is
the single source of truth: its labelled chunks are read verbatim
(via \code{\link[knitr:read_chunk]{knitr::read_chunk()}}) by the openfhe-jss manuscript and the
\code{cvxr-consensus-admm} vignette, so the code displayed there is
exactly the code that produced these results. Find the installed
copy with
\code{system.file("scripts", "cvxr-consensus.R", package = "homomorpheR")}.
}
\seealso{
\link{DLBCL}, \link{DLBCL_gex}
}
\keyword{datasets}
